{
  "name": "bestnotes",
  "version": "1.0.0",
  "description": "BestNotes — Haryana Group D Premium Notes",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "notes": "No npm packages needed for functions — uses Node.js built-ins (https, crypto) only. This keeps the deploy fast and the dependency surface minimal."
}
